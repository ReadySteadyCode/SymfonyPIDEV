{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Commentairedeal</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dateajout</th>
                <td>{{ entity.dateajout|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ entity.contenu }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('commentairedeal') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('commentairedeal_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
