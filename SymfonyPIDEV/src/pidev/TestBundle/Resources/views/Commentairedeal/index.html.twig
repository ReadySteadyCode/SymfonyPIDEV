{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Commentairedeal list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dateajout</th>
                <th>Contenu</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('commentairedeal_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateajout %}{{ entity.dateajout|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.contenu }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('commentairedeal_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('commentairedeal_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('commentairedeal_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
